import React from 'react';
import {observer} from 'mobx-react';

export const TakeRegister = observer(class TakeRegister extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return <h1>This view is still being implemented.</h1>
    }
});
